use serde::Deserialize;
use std::error::Error;
use std::fs::File;
use std::path::Path; // To work with file paths more robustly


pub fn read_securities(file_path: &Path) {
    
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {}
}
